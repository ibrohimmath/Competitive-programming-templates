#pragma GCC optimize("Ofast,unroll-loops")
#include "bits/stdc++.h"

// using namespace std;
using ll = long long;

// #define TESTCASES
void solve(int& t) {

}

int32_t main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(0);
    std::cout.tie(0);

    int tests = 1;
    #ifdef TESTCASES
        std::cin >> tests;
    #endif

    for (int t = 1; t <= tests; ++t) {
        solve(t);
        std::cout << "\n";
    }
}